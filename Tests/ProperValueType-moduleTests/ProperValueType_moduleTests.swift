import XCTest
@testable import ProperValueType_module

final class ProperValueType_moduleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ProperValueType_module().text, "Hello, World!")
    }
}
